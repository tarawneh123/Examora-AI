# سجل التغييرات والإصدارات (CHANGELOG)

## [الإصدار الإنتاجي الشامل والنهائي] - 2026-09-04

### 1. معمارية البيانات وقواعد البيانات (Data Architecture & Database)
- **إضافة كيان المواد الدراسية (Subjects Entity):** تحويل المادة من مجرد نص مكرر إلى كيان مستقل يحمل الرمز، الاسم، اللغة والاتجاه.
- **تطوير جدول حزم الأسئلة (Question Packages):** استبدال جدول الوحدات القديم بجدول حزم مرتبط بالمواد الدراسية مع فرض قيد الفرادة `UNIQUE(subject_id, name)`.
- **نظام اللقطات الثابتة (Immutable Attempt Snapshots):** إنشاء جدول `attempt_snapshots` لحفظ نسخة فورية ومجمدة من الأسئلة والخيارات والعلامات عند بدء المحاولة، لمنع تغير نتائج الطلاب السابقة عند تعديل بنك الأسئلة لاحقاً.
- **سجل التدقيق (Audit Trail):** إضافة جدول `audit_log` لتوثيق جميع العمليات الحساسة (نشر الامتحانات، الحذف، التعديلات، تسليم الطلاب).
- **نظام النسخ الاحتياطي التلقائي:** تشغيل سكريبت `migrate.py` الذي يقوم بعمل نسخة احتياطية مشفرة بختم زمني قبل تطبيق أي تعديلات.
- **دعم التخزين الشبكي المتقدم (9p / POSIX Lock Resiliency):** معالجة أخطاء `disk I/O error` في البيئات السحابية المشتركة عبر طبقة تفاوض ذكية بين التخزين المؤقت والملف الدائم.

### 2. محرك الاستخراج الذكي (OSR Engine)
- **توحيد محرك الاستخراج:** اعتماد `OSR.py` كالمحرك الرسمي الوحيد للنظام.
- **الذكاء اللغوي (Language Awareness):** التعرف التلقائي والتكيف مع امتحانات اللغة العربية (أ-د / RTL) والإنجليزية (A-D / LTR).
- **معالجة التخطيطات المعقدة:** دعم الخيارات المتعددة في نفس السطر (Horizontal Options) والتنسيقات متعددة الأعمدة.
- **منع الإيجابيات الكاذبة (False Positives Prevention):** استبعاد الكلمات الإنجليزية الشائعة (مثل `All`, `Could`, `About`) من أن تُفسر خطأ كخيارات أسئلة.
- **ضبط جودة الاستخراج:** تعيين حالة `NEEDS_REVIEW` افتراضياً لجميع الأسئلة المستخرجة مع احتساب نسبة الثقة `confidence` وتوليد تحذيرات الجودة.

### 3. تجربة المستخدم والواجهات (UX / UI Improvements)
- **القضاء التام على إزعاج التنبيهات (No Toast Spam):** استبدال رسائل الـ Toast المزعجة بحالات مضمنة وتحديث فوري للعدادات وبطاقات حالة واضحة.
- **شريط التنقل الجانبي الاحترافي:** تنظيم الروابط ضمن تبويبات منطقية (الرئيسية، العمل اليومي، إعداد الأسئلة، الإعداد، النظام)، مع قائمة منبثقة متجاوبة لشاشات الهواتف.
- **منشئ الامتحانات الذكي:** حساب عدد الأسئلة تلقائياً عند التحديد دون إجبار المستخدم على إدخال الرقم يدوياً.
- **منتقي الألوان الحقيقي:** استخدام `<input type="color">` مع تطبيق فوري للمتغيرات عبر CSS Variables.
- **قائمة خطوط منتقاة:** إتاحة خطوط عربية ولاتينية عالية الجودة (Cairo, Tajawal, Noto Sans Arabic, Inter, Roboto).

### 4. الطباعة الرسمية وملفات PDF
- **ضبط ترويسة A4 الوزارية:** ظهور الترويسة في الصفحة الأولى فقط (Page 1 Only).
- **إزالة الفراغ العشوائي في الصفحات اللاحقة:** تبدأ الأسئلة في الصفحة الثانية مباشرة من أعلى الصفحة (Zero Blank Gap).
- **ثبات اللغة والاتجاه:** في امتحانات اللغة الإنجليزية، تظل الترويسة بالعربية واليمين (RTL) دون تكسر الحروف، وأسئلة الامتحان بالإنجليزية (LTR).
- **إخفاء الأدوات غير الضرورية:** إخفاء الأزرار وشريط التنقل والمؤقتات في الطباعة.

### 5. الأمان وفصل السياقات (Application Security & Isolation)
- **عزل جلسات الطلاب عن الإدارة:** منع إعادة توجيه أخطاء الإدارة للطلاب أو العكس.
- **حماية أمان النتائج (IDOR Prevention):** منع أي طالب من الوصول لنتائج أو أوراق امتحانات الطلاب الآخرين عبر تعديل الرابط.
- **حماية الامتحانات بكلمة مرور:** دعم تشفير وتحقق آمن لكلمات مرور دخول الامتحانات.
- **معالجة الملفات المرفوعة:** التحقق الصارم من امتداد الملفات ونوع MIME وحجم الملف لمنع رفع وتنفيذ ملفات ضارة.

## [تحديث الصيانة وحل مشاكل تجربة المستخدم والترويسة] - 2026-09-04

### 1. إصلاح مشكلة التحديث المتكرر اللانهائي (Infinite Refresh Loop Fix)
- **معالجة تكرار الطلبات في صفحة بنك الأسئلة:** إزالة استدعاء `dispatchEvent(new Event('change'))` التلقائي عند تحميل الصفحة في `initProgressiveFiltering()` داخل `main.js`، واستبداله بدالة فلترة صامتة تمنع إرسال النماذج بشكل متكرر دون إذن المستخدم.
- **إلغاء `onchange="this.form.submit()"` العشوائي:** استبدال الإرسال التلقائي في قائمة بنك الأسئلة بزر بحث وتصفية صريح ومباشر ("تصفية وبحث").

### 2. الترويسة الوزارية الرسمية المطابقة لمعايير وزارة التربية والتعليم
- **ظهور إشارة وزارة التربية والتعليم:** إدراج الشعار الرسمي لوزارة التربية والتعليم والمملكة الأردنية الهاشمية في العمود الأيمن من الترويسة بشكل افتراضي وثابت.
- **تطابق الترويسة مع النموذج الوزاري المرفق:** مطابقة التصميم المعتمد من 3 أعمدة (اليمين: شعار الوزارة والمديرية والصف واسم الطالب؛ الوسط: المملكة الأردنية الهاشمية، وزارة التربية والتعليم، مدرسة ذرى المجد/اسم المدرسة، واسم الامتحان والفصل الدراسي؛ اليسار: شعار المدرسة والتاريخ والعلامة ومدة الامتحان).

### 3. إزالة التنبيهات البدائية واستبدالها بنظام حديث (Toast & Modal System)
- **إلغاء نوافذ المتصفح الافتراضية (`alert`):** إنشاء نظام إشعارات عائمة أنيقة (`custom-toast`) مع أيقونات ملونة، ومؤقتات اختفاء تلقائي، ونافذة تنبيه مخصصة (`showAlertModal`).

### 4. ترقية محرك الاستخراج الذكي (OSR) وزيادة الدقة
- **معالجة مسبقة متقدمة للصور (Preprocessing Pipeline):** تكبير الدقة التلقائي إلى 300 DPI مكافئ، تحسين التباين والحدة، وإجراء عتبة تكيفية بالأبيض والأسود (Binarization) لتسهيل استخراج النصوص العربية المشوشة.
- **استراتيجية القراءة متعددة التمريرات (Multi-Pass PSM):** التنقل الذكي بين أوضاع التقسيم لضمان التقاط الأسئلة والخيارات الأفقية والعمودية بدقة عالية.

### 5. تبسيط إدارة المواد الدراسية وتعديل المسميات
- **إلغاء تصنيف المواد (أساسية / ثانوية):** تمكين المعلمة من إدخال اسم المادة فقط وإضافتها فوراً، وحذف أي مادة لا ترغب بتدريسها بحرية تامة دون قيود.
- **تعديل مسمى "الحزم" إلى "الوحدات":** اعتماد مسمى "الوحدة الدراسية" / "الوحدات" في كافة القوائم والجداول وشاشات الاستخراج والامتحانات.

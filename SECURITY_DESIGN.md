# وثيقة التصميم الأمني وحماية البيانات (SECURITY DESIGN & THREAT MODEL)
**المشروع:** Examora AI  
**المستوى:** Enterprise-Grade Security Architecture  
**التاريخ:** 2026-09-08

---

## 1. أمن مفتاح الإجابة (Answer Key Confidentiality Architecture)

أحد أهم المتطلبات الأساسية هو ضمان استحالة وصول الطالب للإجابات الصحيحة بأي شكل من الأشكال:

```
[ Teacher Local App ] 
        |
        |  1. إرسال الامتحان (قسمين مستقلين)
        v
[ Online Cloud Backend ]
        |
        +---> (أ) الأسئلة العامة (نص، خيارات، علامات) ---> تخزن في online_exams.questions_json
        |                                                 (متاحة عبر GET/POST للطلاب)
        |
        +---> (ب) مفتاح الإجابة السري (Correct Answers) ---> تخزن في online_answer_keys
                                                           (معزولة خادمياً - غير متاحة بأي مسار API)
```

### إجراءات الحماية الصارمة لمفتاح الإجابة:
1. **انعدام المسارات البرمجية:** لا يوجد أي مسار API عام (Public Endpoint) يمكنه الاستعلام عن جدول `online_answer_keys` أو إرجاع محتواه.
2. **استحالة الاستخراج من المتصفح:** فحص أدوات المطورين (DevTools)، أو تبويب الشبكة (Network Tab)، أو ملفات JavaScript، أو LocalStorage لن يكشف أي إجابة نموذجية لأنها لم تغادر الخادم السحابي أصلاً.
3. **التصحيح الداخلي فقط:** يتم استدعاء مفتاح الإجابة في الذاكرة المؤقتة للخادم فقط عند وصول طلب التسليم `POST /api/public/exam/submit` لمقارنة إجابات الطالب واعتماد العلامة، ثم إتلاف السياق فوراً.

---

## 2. توثيق المعلم وتدوير المفاتيح (Teacher Auth & Secret Management)

1. **بروتوكول التوثيق السحابي:**
   * اتصال التطبيق المحلي بالخادم السحابي محمي بترويسة:
     `Authorization: Bearer <TEACHER_ONLINE_SECRET>`
   * يتم التحقق في الخادم باستخدام مقارنة ثنائية ثابتة الزمن لمنع هجمات التوقيت:
     `hmac.compare_digest(provided_token, configured_secret)`
2. **عزل الأسرار وعدم تضمينها بالواجهات:**
   * مفتاح `TEACHER_ONLINE_SECRET` لا يوضع نهائياً في ملفات HTML أو JavaScript أو داخل مستودع Git أو على استضافة Firebase Hosting.
   * يُحقن المفتاح في الخادم السحابي كمتغير بيئة سري (`Environment Variable`).
   * يُحفظ المفتاح محلياً في ملف إعدادات مشفر بجهاز المعلم أو يمرر كمتغير بيئي محلي `TEACHER_ONLINE_SECRET`.
3. **آلية تدوير المفتاح (Key Rotation):**
   * عند الحاجة لتدوير المفتاح (في حال الاشتباه في تسريبه)، يتم تعديل قيمة المتغير السحابي وإعادة تشغيل الخادم السحابي، وتحديث القيمة في إعدادات التطبيق المحلي دون أي توقف أو تأثير على قواعد البيانات.

---

## 3. حماية المحاولات ومنع انتحال الهوية (IDOR & Attempt Security)

* **المشكلة الشائعة:** اعتماد الخوادم على أرقام محاولات متسلسلة (`attempt_id=1, 2, 3`)، مما يسمح لطالب خبيث بتغيير الرقم وحفظ إجابات في محاولة زميله.
* **الحل المعماري في Examora AI:**
  * عند بدء المحاولة، يولد الخادم رمز جلسة عشوائي مشفر بقوة 256 بت:
    `attempt_token = secrets.token_urlsafe(32)`
  * يتم ربط `attempt_token` بسجل المحاولة في قاعدة البيانات.
  * كل استدعاء لمساري `autosave` و `submit` يتطلب تمرير `attempt_token`.
  * يقوم الخادم بمطابقة صريحة:
    ```python
    if not attempt or not hmac.compare_digest(attempt['attempt_token'], provided_token):
        return abort(403)
    ```
  * هذا يجعل التخمين أو التعديل مستحيلاً إحصائياً (فضاء احتمالات يتجاوز $2^{256}$).

---

## 4. الحصانة ضد التلاعب بالوقت والعلامات (Anti-Tampering)

1. **منع تزوير العلامة (Client-Side Score Manipulation):**
   * الخادم السحابي لا يقبل أي حقل علامة أو نسبة مئوية مرسل من المتصفح، ويتم تجاهل أي جسم طلب يحتوي على `score` أو `mark`. الحساب خادمي حصراً.
2. **منع التلاعب بالمؤقت (Server-Side Timer Enforcement):**
   * يُسجل الخادم تاريخ البداية `started_at` والموعد النهائي الصارم `server_deadline = started_at + duration`.
   * في كل طلب حفظ أو تسليم، يقارن الخادم الساعة الحالية بساعة الخادم وليس ساعة متصفح الطالب.
   * يُمنح هامش شبكي مقداره 15 ثانية فقط لتعويض بطء الاتصال، وأي طلب بعد ذلك يُرفض فوراً وتتحول المحاولة تلقائياً إلى `EXPIRED`.

---

## 5. مصفوفة التهديدات الأمنية الشاملة (Threat Model Matrix)

| متجه الهجوم (Attack Vector) | تقييم المخاطر | الدفاع المعماري المطبق في Examora AI |
| :--- | :---: | :--- |
| **كشف قاعدة بيانات الأستاذ** | حرج جداً | عزل تام؛ SQLite لا يرفع للسحابة ولا يفتح أي منفذ خارجي إطلاقاً. |
| **انتحال محاولات الطلاب (IDOR)** | عالي | ربط كل محاولة برمز `attempt_token` مشفر عشوائي يُلزم بتقديمه عند كل طلب. |
| **تخمين رموز الامتحانات المنشورة** | متوسط | استخدام رموز نشر عشوائية قوية (`secrets.token_urlsafe(24)`). |
| **هجمات الحرمان والإغراق (Brute Force / DDoS)** | عالي | تفعيل Rate Limiting بمعدل 60–120 طلباً في الدقيقة لكل IP وتحديد حجم الـ Payload بـ 100KB. |
| **التلاعب بالوقت** | عالي | حساب الوقت خادمياً ورفض أي تمديد محلي من المتصفح. |
| **حقن الأوامر وقواعد البيانات (SQLi)** | حرج | استخدام Prepared Statements و Parameterized Queries 100%. |
| **استغلال سياسات CORS** | متوسط | حصر النطاق المسموح به في الإنتاج على `https://yt-c-c.web.app` فقط. |
| **تسريب بيانات الطلاب الآخرين** | عالي | إرسال بيانات الامتحان المحددة فقط للطالب دون أي سجلات لطلاب آخرين. |

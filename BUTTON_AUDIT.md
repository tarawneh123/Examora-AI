# تدقيق شامل للأزرار والإجراءات التفاعلية (Button & Action Audit)

يوثق هذا التدقيق كافة الأزرار، والروابط، والنماذج، والإجراءات المنفذة في واجهة المستخدم، للتأكد من وجود معالج برمجي (Handler) ونقطة نهاية (Endpoint) وتحديث فعلي لقاعدة البيانات.

---

| الصفحة | الزر / الإجراء | النوع | المسار المرتبط (Endpoint) | الطريقة (Method) | النتيجة / الأثر على النظام | حالة التدقيق |
|:---|:---|:---:|:---|:---:|:---|:---:|
| **لوحة القيادة** | "+ إنشاء امتحان جديد" | زر رئيسي | `/exams/new` | GET | فتح منشئ الامتحانات | **PASS** |
| **لوحة القيادة** | "⚡ استخراج أسئلة" | زر ثانوي | `/import` | GET | فتح مركز OSR | **PASS** |
| **لوحة القيادة** | "إدارة" (في جدول الامتحانات) | زر جدول | `/exams/<id>/manage` | GET | فتح مركز إدارة الامتحان | **PASS** |
| **لوحة القيادة** | "🖨️ طباعة" | رابط نافذة | `/print/exam/<id>` | GET | فتح ورقة الطباعة A4 | **PASS** |
| **المواد الدراسية** | "+ إضافة مادة جديدة" | زر Modal | `/subjects/add` | POST | إضافة مادة بجدول `subjects` | **PASS** |
| **المواد الدراسية** | "حذف" مادة | زر نموذج | `/subjects/delete/<id>` | POST | حذف المادة المخصصة بأمان | **PASS** |
| **حزم الأسئلة** | "+ إضافة حزمة أسئلة" | زر Modal | `/packages/add` | POST | إنشاء حزمة بجدول `question_packages` | **PASS** |
| **حزم الأسئلة** | "عرض الأسئلة" | رابط تصفية | `/questions?package_id=<id>` | GET | عرض أسئلة الحزمة في البنك | **PASS** |
| **حزم الأسئلة** | "حذف" حزمة | زر نموذج | `/packages/delete/<id>` | POST | حذف الحزمة مع إبقاء الأسئلة | **PASS** |
| **استخراج OSR** | "+ حزمة جديدة" (فورية) | زر AJAX | `/api/packages/add` | POST | إضافة حزمة فورية وتحديدها بالقائمة | **PASS** |
| **استخراج OSR** | "+ إضافة سؤال يدوي" | زر سياقي | `/questions/save` | POST | إضافة سؤال يرث المادة والحزمة | **PASS** |
| **استخراج OSR** | "⚡ بدء الاستخراج والتحليل" | زر إرسال | `/import/process` | POST | استخراج الأسئلة بحالة `NEEDS_REVIEW` | **PASS** |
| **مراجعة الأسئلة** | "✓ اعتماد الأسئلة المحددة" | زر جماعي | `/review/bulk` | POST | ترقية الأسئلة المحددة إلى `APPROVED` | **PASS** |
| **مراجعة الأسئلة** | "رفض المحددة" | زر جماعي | `/review/bulk` | POST | تعيين الحالة إلى `REJECTED` | **PASS** |
| **مراجعة الأسئلة** | "حذف المحددة" | زر جماعي | `/review/bulk` | POST | حذف الأسئلة من جدول `questions` | **PASS** |
| **مراجعة الأسئلة** | "✓ اعتماد السؤال" | زر فردي | `/questions/approve/<id>` | GET | اعتماد السؤال المنفرد | **PASS** |
| **مراجعة الأسئلة** | "تعديل" سؤال | زر Modal | `/questions/save` | POST | تعديل نص السؤال وخياراته وعلامته | **PASS** |
| **بنك الأسئلة** | "+ إضافة سؤال يدوي" | زر Modal | `/questions/save` | POST | إنشاء سؤال معتمد ببنك الأسئلة | **PASS** |
| **بنك الأسئلة** | "⇩ تصدير Excel (CSV)" | زر تنزيل | `/questions/export` | GET | تنزيل ملف CSV بترميز عربي | **PASS** |
| **بنك الأسئلة** | "بحث" و "تصفية" | نموذج فلترة | `/questions` | GET | تصفية الأسئلة في الخادم | **PASS** |
| **بنك الأسئلة** | "نقر مزدوج" على الصف | حدث فوري | `ondblclick` | JS | فتح نافذة التعديل السريع فوراً | **PASS** |
| **منشئ الامتحان** | تحديد السؤال (Checkbox) | حدث فوري | `.exam-question-checkbox` | JS | تحديث عداد الأسئلة فورياً تلقائياً | **PASS** |
| **منشئ الامتحان** | "✓ إنشاء الامتحان وحفظه" | زر إرسال | `/exams/create/process` | POST | إنشاء الامتحان وربط أسئلته | **PASS** |
| **إدارة الامتحان** | "نشر / إيقاف النشر" | زر تبديل | `/exams/<id>/toggle` | POST | التحقق من الشروط وتبديل الحالة | **PASS** |
| **إدارة الامتحان** | "نسخ" الامتحان | زر إجراء | `/exams/<id>/copy` | POST | تكرار الامتحان دون نسخ المحاولات | **PASS** |
| **إدارة الامتحان** | "حذف الامتحان" | زر تحذيري | `/exams/<id>/delete` | POST | حذف ذري للامتحان مع صيانة البنك | **PASS** |
| **إدارة الامتحان** | "🖨️ طباعة ورقة الامتحان" | رابط نافذة | `/print/exam/<id>` | GET | طباعة رسمية A4 بترويسة ص1 فقط | **PASS** |
| **إدارة الامتحان** | "📄 تحميل PDF" | زر تنزيل | `/pdf/exam/<id>` | GET | تصدير PDF مباشر | **PASS** |
| **إدارة الامتحان** | "عرض النتيجة" (للطلاب) | زر تقرير | `/results/attempt/<id>` | GET | استعراض ورقة إجابة الطالب | **PASS** |
| **سجل الطلاب** | "+ إضافة طالب جديد" | زر Modal | `/students/add` | POST | التحقق من الاسم الرباعي وحفظه | **PASS** |
| **سجل الطلاب** | "حذف" طالب | زر تحذيري | `/students/delete/<id>` | POST | حذف الطالب وسجلاته ومحاولاته | **PASS** |
| **مكتبة الوسائط** | "+ رفع صور جديدة" | زر Modal | `/media/upload` | POST | رفع وتخزين الصور المعتمدة | **PASS** |
| **مكتبة الوسائط** | "حذف" صورة | زر إجراء | `/media/delete/<filename>` | POST | إزالة الصورة من القرص | **PASS** |
| **النتائج والتقارير** | "⇩ تصدير Excel (CSV)" | زر تنزيل | `/results/export` | GET | تنزيل كشف العلامات المجمع | **PASS** |
| **النسخ الاحتياطي** | "💾 إنشاء نسخة احتياطية" | زر إرسال | `/backup/create` | POST | نسخ قاعدة البيانات فورياً وتوثيقها | **PASS** |
| **النسخ الاحتياطي** | "تنزيل الملف" | رابط تنزيل | `/backup/download/<name>` | GET | تنزيل ملف `.db` المحفوظ | **PASS** |
| **بوابة الطالب** | "تسجيل الدخول" | زر إرسال | `/student/login/post` | POST | مصادقة الطالب وتوجيهه للامتحانات | **PASS** |
| **شاشة الامتحان** | اختيار خيار الإجابة | حدث فوري | `.opt-input` | AJAX | حفظ تلقائي Debounced Autosave | **PASS** |
| **شاشة الامتحان** | "✓ إنهاء وتسليم الامتحان" | زر تأكيد | `/student/exam/<id>/submit` | POST | احتساب العلامة واعتماد المحاولة | **PASS** |
| **شاشة النتيجة** | "🖨️ طباعة كشف النتيجة" | رابط نافذة | `/print/attempt/<id>` | GET | طباعة رسمية لنتيجة الطالب | **PASS** |

# مسار تدفق البيانات (Data Flow Architecture)

يوضح هذا المستند دورة حياة البيانات وانتقالها بين مختلف الكيانات والطبقات في منصة الاختبارات التعليمية من مرحلة الاستخراج الأولي حتى أرشفة النتائج والطباعة.

---

## 🔄 المخطط التفصيلي لتدفق البيانات

```
1. إعداد المساق والمصدر
   [اختيار المادة الدراسية (Subject)] 
          + [اختيار الحزمة (Package)]
          + [رفع الصور/الملف أو لصق النص]
                     │
                     ▼
2. المعالجة والاستخراج الذكي
   [محرك OSR: تحليل التخطيط + كشف الاتجاه + منع الإيجابيات الكاذبة]
                     │
                     ▼
3. مسودة الأسئلة بانتظار المراجعة
   [جدول questions: status = 'NEEDS_REVIEW', approved = 0, confidence]
                     │
                     ▼
4. بوابة ضبط الجودة (QC Review)
   [مراجعة المعلمة: تعديل / رفض / اعتماد]
                     │
         ┌───────────┴───────────┐
         ▼                       ▼
    [APPROVED = 1]         [REJECTED = 0]
         │                       │
         ▼                       ▼
5. بنك الأسئلة المعتمدة     [حفظ السجل أو الحذف]
   [مستودع الأسئلة الجاهزة للاختبار]
                     │
                     ▼
6. إنشاء ورقة الامتحان
   [اختيار الأسئلة المعتمدة] ──► [حساب تلقائي لعدد الأسئلة والعلامة]
                     │
                     ▼
7. نشر الامتحان وتحديد الصلاحيات
   [status = 'PUBLISHED', كلمة مرور مشفرة، التحقق من اتساق المادة]
                     │
                     ▼
8. دخول الطالب وبدء المحاولة
   [تحقق الطالب + التحقق من كلمة المرور]
                     │
                     ▼
9. أخذ اللقطة الثابتة المجمدة (Immutable Snapshot)
   [نسخ الأسئلة والخيارات والعلامات إلى جدول attempt_snapshots]
                     │
                     ▼
10. التقديم والحفظ التلقائي (Autosave)
    [تغيير الإجابة] ──► [تأخير Debounce 300ms] ──► [تحديث جدول answers]
                     │
                     ▼
11. التسليم النهائي واحتساب النتيجة
    [status = 'SUBMITTED', احتساب العلامة والنسبة من واقع اللقطة حصراً]
                     │
                     ▼
12. كشوفات النتائج والطباعة الرسمية
    [طباعة ورقة الامتحان أو النتيجة استناداً إلى اللقطة الموثقة تاريخياً]
```

---

## 🔒 قواعد اتساق البيانات (Data Integrity Rules)

1. **قيد اتساق المادة (Subject Integrity):**
   - لا يمكن ربط حزمة أسئلة بمادة غير موجودة.
   - لا يمكن تضمين أسئلة من مادة دراسية مختلفة في امتحان مادة أخرى.

2. **عزل النتائج التاريخية (Snapshot Immutability):**
   - بمجرد بدء المحاولة، تستمد الإجابات وعلامات الطالب تصحيحها من `attempt_snapshots`.
   - تعديل نص السؤال أو تغيير الخيار الصحيح في بنك الأسئلة لا يمس المحاولات المسلمة نهائياً.

3. **الحذف الآمن للامتحانات (Transactional Exam Deletion):**
   - حذف الامتحان يمسح سجلات الامتحان ومحاولاته ولقطاته وإجاباته التابعة له في معاملة ذرية واحدة (Atomic Transaction)، مع بقاء أسئلة بنك الأسئلة المعتمدة محفوظة دون حذف.

# وثيقة الفحص الشامل للمشروع (PROJECT AUDIT)
**مشروع:** Examora AI (منصة الاختبارات والتقييم الذكية)  
**المرحلة:** PHASE 0 — PROJECT DISCOVERY  
**التاريخ:** 2026-09-08  
**المرجع:** كبير مهندسي النظم وأمن المعلومات (Lead System Architect & Security Engineer)

---

## 1. المعمارية الحالية للنظام (Current Architecture)

النظام الحالي يعمل بنموذج هجين أولي غير مكتمل بين تطبيق محلي واستضافة واجهة أمامية:

```
                  ┌────────────────────────────────────────┐
                  │            المتصفحات والإنترنت         │
                  └───────────────────┬────────────────────┘
                                      │
                         (طلب صفحات الويب الثابتة)
                                      ▼
                        ┌───────────────────────────┐
                        │     Firebase Hosting      │
                        │    (https://yt-c-c.web.app)│
                        │ ├── index.html            │
                        │ ├── exam.html             │
                        │ └── js/exam.js            │
                        └─────────────┬─────────────┘
                                      │
              (فجوة الاتصال: يحاول الاتصال بـ localhost:8765)
                                      │
                                      ▼
       ====================== جهاز المعلم المحلي ======================
      │                                                               │
      │   ┌───────────────────────────────────────────────────────┐   │
      │   │               خادم التطبيق المحلي (Flask)              │   │
      │   │                 (app.py / mini_flask.py)              │   │
      │   │                      Port: 8765                       │   │
      │   └───────────────┬───────────────────────┬───────────────┘   │
      │                   │                       │                   │
      │                   ▼                       ▼                   │
      │     ┌────────────────────────┐  ┌──────────────────────────┐  │
      │     │  طبقة الخدمات والمنطق  │  │  واجهات إدارة المعلم     │  │
      │     │  (examora_service.py)  │  │  (Templates & Routes)    │  │
      │     └─────────────┬──────────┘  └──────────────────────────┘  │
      │                   │                                           │
      │                   ▼                                           │
      │     ┌──────────────────────────────────────────────────┐      │
      │     │          قاعدة البيانات المحلية (SQLite)          │      │
      │     │            exam_data/exam_platform.db            │      │
      │     └──────────────────────────────────────────────────┘      │
      │                                                               │
       ===============================================================
```

### مكونات النظام الحالية:
1. **خادم الويب والـ Backend المحلي:** مبني بـ Python عبر إطار العمل المكتفي ذاتياً `mini_flask.py` المتوافق مع Flask، ويعمل على المنفذ `8765`.
2. **قاعدة البيانات المحلية:** SQLite (`exam_data/exam_platform.db`)، تحتوي على 22 جدولاً تشمل بيانات المدرسة، المعلم، الطلاب، بنك الأسئلة، الامتحانات، المحاولات، اللقطات، والتدقيق الرقابي.
3. **الواجهة الأمامية السحابية (Firebase Hosting):** مرفوعة على النطاق `https://yt-c-c.web.app/#` ومصدرها مجلد `firebase_landing/`.
4. **بوابة المعلم (Teacher UI):** واجهة ويب محلية بنظام لوحة تحكم ونوافذ جانبية ومودالات تفاعلية تتصل مباشرة بالـ Backend المحلي.
5. **بوابة الطالب المحلية (Local Student UI):** مسارات محلية (`/student/login`، `/student/exam/<id>`).
6. **طبقة النشر الحالية:** طبقة محلية (Local Staging) تسجل الامتحانات المنشورة داخل جداول SQLite المحلية (`published_exams`, `published_exam_attempts`).

---

## 2. جرد وتحليل أهم الملفات (Key Files Inventory)

| اسم الملف | وظيفته ودوره في المنظومة | الاعتماديات (Dependencies) | نطاق العمل (Local / Online) | هل يحتاج تعديل؟ | ملاحظات السلامة والحماية |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **`app.py`** | خادم التطبيق المحلي الرئيسي، إدارة التوجيه، التوثيق، والقوالب. | `mini_flask`, `OSR`, `examora_service`, `examora_routes`, SQLite | **Local فقط** | **تعديل طفيف جداً** (ربط إعدادات النشر السحابي) | **ممنوع إتاحته للإنترنت أو كشف منافذه مباشرة.** |
| **`examora_service.py`** | الطبقة الخدمية لمنطق العمليات، الامتحانات التعويضية، الحذف، والمزامنة. | SQLite, `hashlib`, `secrets`, `datetime`, `urllib` | **Local فقط** | **نعم** (تحديث عميل المزامنة والنشر السحابي الحقيقي) | يحافظ على تكامل البيانات المحلية وعمليات التدقيق الرقابي. |
| **`examora_routes.py`** | مسارات المنتدى، الشكاوى، والنقاط البرمجية الموسعة. | `mini_flask`, `examora_service` | **Local فقط** | **تعديل طفيف** (فصل نقاط الطلاب الخارجية إلى خادم السحابة) | لا يجوز تعريض نقاط المعلم للإنترنت دون مصادقة. |
| **`mini_flask.py`** | محرك WSGI متكامل وخفيف بديل لـ Flask للبيئة المعزولة. | Standard Library, `jinja2` | **مشترك (Local & Online)** | **لا** | مستقر ومجرب بالكامل. |
| **`migrate.py`** | مدير الترحيل الآمن والنسخ الاحتياطي لقاعدة البيانات المحلية. | SQLite, `shutil`, `pathlib` | **Local فقط** | **لا** | تم ترقيته للإصدار v31 بنجاح تام. |
| **`OSR.py`** | محرك التعرف الذكي واستخراج الأسئلة من الصور ومستندات Word. | `pytesseract`, `PIL`, `zipfile`, `xml.etree` | **Local فقط** | **ممنوع تعديله** | محرك محلي لمعالجة وثائق المعلم الخاصة. |
| **`exam_platform.db`** | مستودع بيانات الأستاذ والمدرسة والنتائج والسجلات. | SQLite 3 | **Local حصراً** | **ممنوع رفعه نهائياً** | المصدر النهائي والوحيد؛ غير متاح للإنترنت إطلاقاً. |
| **`firebase_landing/index.html`** | واجهة الهبوط والبوابة السحابية المرفوعة على Firebase. | Firebase SDK Compat, CSS, JS | **Online** | **نعم** (ضبط مسار التوجيه إلى قاعة الامتحان برمز النشر) | واجهة ثابتة بدون أي أسرار خادمة. |
| **`firebase_landing/exam.html`** | قاعة الامتحان السحابية للطلاب عبر الإنترنت. | `main.css`, `exam.css`, `config.js`, `exam.js` | **Online** | **نعم** (ربط مع متغيرات البيئة السحابية) | خالية تماماً من الإجابات النموذجية. |
| **`firebase_landing/js/exam.js`** | منطق العميل لإجراء الامتحان السحابي والعداد والحفظ. | Fetch API, DOM | **Online** | **نعم** (إزالة أي إشارة لـ localhost، ربط Attempt Token) | التحقق دوماً من سلامة المدخلات خادمياً. |
| **`firebase_landing/js/config.js`** | ملف ضبط عنوان الـ Online API للواجهة السحابية. | Vanilla JS | **Online** | **نعم** (تجهيز الرابط الإنتاجي القابل للتبديل) | لا يحتوي على أي مفاتيح سرية خادمة. |
| **`online_backend/app.py`** | **[ملف جديد مستحدث]** خادم الـ API السحابي المنفصل للإنترنت. | `mini_flask`/Flask, `database.py`, `config.py` | **Online فقط** | **نعم** (إنشاء وتدقيق) | يستقبل فقط الامتحانات المنشورة ويصحح خادمياً. |
| **`online_backend/database.py`** | مدير قاعدة بيانات السحابة المؤقتة (PostgreSQL / SQLite). | Database Driver | **Online فقط** | **نعم** (إنشاء وتدقيق) | قاعدة بيانات سحابية مستقلة تماماً عن جهاز المعلم. |

---

## 3. فحص قاعدة البيانات المحلية (Local Database Audit)

تحتوي قاعدة البيانات المحلية `exam_data/exam_platform.db` على **22 جدولاً** مترابطاً:

### أ. الجداول الحساسة (Strictly Private - Non-Exposable):
* **`authorized_teachers`** (1 سجل): يحوي بيانات تسجيل دخول المعلم، كلمات المرور، وربط الأجهزة، والاشتراكات.
* **`settings`** (45 سجلاً): إعدادات النظام، كلمات مرور المدير، الرسائل التشجيعية، وبيانات الهوية الرسمية.
* **`audit_log`** (464 سجلاً): سجل التدقيق الرقابي الكامل لجميع عمليات النظام والتعديلات.
* **`complaints_suggestions`** و **`notifications`**: بيانات التواصل الداخلي والشكاوى وملاحظات الإدارة.
* **`forum_topics`**, **`forum_replies`**, **`forum_likes`**: منتدى الأساتذة التشاركي.

### ب. الجداول المتعلقة بالامتحانات والأسئلة:
* **`exams`** (119 امتحاناً): يحتوي على الامتحانات العادية والتعويضية (`exam_kind`) ورموز الدخول ومحددات الوقت.
* **`questions`** (166 سؤالاً): بنك الأسئلة الشامل (النصوص، الخيارات، الإجابة الصحيحة `correct`، العلامات).
* **`exam_questions`** (68 سجلاً): جداول الربط بين الامتحانات والأسئلة وترتيب ظهورها.
* **`question_packages`** (سجلان): تصنيفات الوحدات والدروس الدراسية.
* **`subjects`** (6 مواد): المواد المعتمدة واتجاهاتها ولغاتها.

### ج. الجداول المتعلقة بالطلاب والنتائج:
* **`students`** (12 طالباً): السجل المدني، الأرقام الوطنية، الصفوف، والشعب.
* **`student_subjects`** (29 سجلاً): جدول التحقق الحاسم لتقييد دخول الطالب فقط للامتحانات المسجل بها.
* **`student_exam_overrides`** (3 سجلات): أذونات المحاولات الإضافية الممنوحة من المعلم.
* **`attempts`** (83 محاولة): سجل المحاولات، العلامات، التقديرات، وحالات الاعتماد الرسمي (`is_official`).
* **`attempt_snapshots`** (51 لقطة): النسخ المتطابقة غير القابلة للتغيير التي قدم الطالب امتحانه بناءً عليها.
* **`answers`** (27 إجابة): الإجابات التفصيلية الموثقة لكل سؤال.

### د. جداول التجهيز للنشر (Local Staging):
* **`published_exams`**: جدول التوثيق المحلي للامتحانات التي تم إرسالها للسحابة.
* **`published_exam_attempts`**: جدول استلام المحاولات المزامنة من السحابة.

---

## 4. منظومة المصادقة والصلاحيات (Authentication & Authorization)

1. **توثيق المعلم (Teacher Authentication):**
   * يعتمد على مسار `/api/auth/session` و `/api/auth/firebase-session`.
   * يتم التحقق من كلمات المرور المحلية بتجزئة `PBKDF2-HMAC-SHA256` أو التحقق من صحة `Firebase ID Token` خادمياً.
   * الجلسة تُدار عبر Signed Cookies (`session['admin_logged_in']`, `session['is_super_admin']`, `session['teacher_id']`).
   * **الحماية:** مسارات إدارة النظام والامتحانات محمية بالتحقق من الجلسة والصلاحيات.

2. **توثيق الطالب المحلي (Local Student Authentication):**
   * عبر الرقم الوطني أو اسم المستخدم وكلمة المرور ورمز الامتحان.
   * يتم تسجيل الطالب في جلسة مشفرة محلياً (`session['student_id']`).

3. **توثيق الطالب السحابي (Online Student Authentication):**
   * الدخول يتم حصراً عبر (رابط الامتحان المنشور + الرقم الوطني).
   * يتم التحقق خادمياً من وجود الرقم الوطني ضمن قائمة `allowed_students` الخاصة بالامتحان المنشور.
   * يُمنح الطالب رمز جلسة محاولة مشفر وغير قابل للتخمين (`attempt_token = secrets.token_urlsafe(32)`) يُلزم بإرساله في كل عملية حفظ أو تسليم لمنع هجمات انتحال المحاولات (IDOR).

---

## 5. حالة الـ Online الحالية (Current Online Reality Check)

| المكون | هل هو حقيقي Online؟ | هل هو محلي (Local Staging)؟ | الوصف الدقيق للواقع البرمجي الحالي |
| :--- | :---: | :---: | :--- |
| **واجهة Firebase Hosting** | **نعم (حقيقي)** | لا | الواجهة مرفوعة فعلياً على `https://yt-c-c.web.app/#`. |
| **قاعدة بيانات `published_exams`** | لا | **نعم (محلي)** | الجدول موجود فقط داخل SQLite المحلي في جهاز الأستاذ. |
| **دوال `publish_exam_online`** | لا | **نعم (محلي)** | تقوم حالياً بحفظ البيانات محلياً فقط أو استدعاء خادم وهمي/محلي. |
| **نقاط `/api/public/exam/...`** | لا | **نعم (محلي)** | الكود موجود داخل خادم Flask المحلي وغير متاح خارج شبكة الأستاذ. |
| **رابط الاتصال في `exam.js`** | لا | **نعم (محلي)** | يشير إلى `localhost:8765` أو منفذ محلي ولا يعمل عبر الإنترنت. |
| **مزامنة وحذف النتائج (Sync/Purge)** | لا | **نعم (محلي)** | تعمل فقط بين جداول SQLite المحلية ولا تتصل بخادم سحابي خارجي. |

---

## 6. المشاكل التقنية المرصودة (Current Architecture Gaps)

1. **فجوة خادم الـ Online API:** لا يوجد خادم سحابي مستقل (Cloud Backend) يعمل على نطاق عام يستقبل طلبات الطلاب القادمة من Firebase Hosting.
2. **فجوة قاعدة البيانات السحابية:** عدم وجود قاعدة بيانات سحابية مستقلة (Cloud PostgreSQL / Cloud DB) تستضيف فقط الامتحانات المنشورة وإجابات الطلاب مؤقتاً.
3. **مشكلة `localhost` في ملفات الواجهة:** محاولة `exam.js` الاتصال بـ `localhost:8765` تجعل الواجهة المنشورة عاجزة عن العمل لأي مستخدم من خارج جهاز الأستاذ.
4. **سياسات CORS:** غياب ضبط صارم لسياسات CORS يحدد ويحصر استقبال الطلبات فقط من دومين `https://yt-c-c.web.app`.
5. **غياب قناة التوثيق السحابية بين المعلم والخادم:** عند نشر الامتحان، يجب أن يرسل التطبيق المحلي حزمة النشر مع مفتاح سري موثوق (`TEACHER_API_KEY`) إلى الخادم السحابي.

---

## 7. نموذج التهديدات والمخاطر الأمنية (Threat Model)

| التهديد الأمني (Threat) | مستوى الخطر | آلية الحماية المعمارية المقررة (Mitigation) |
| :--- | :---: | :--- |
| **1. كشف قاعدة بيانات الأستاذ (Local DB Exposure)** | **حرج جداً (Critical)** | **عزل تام:** قاعدة البيانات المحلية لا ترفع ولا تفتح لها أي منافذ، والاتصال صادر فقط من جهاز المعلم إلى السحابة (Outbound-only Sync). |
| **2. تسريب الإجابات النموذجية (Answer Key Leak)** | **حرج جداً (Critical)** | **الفصل الصارم:** إزالة الإجابات الصحيحة تماماً من حزمة الأسئلة المنشورة. مفتاح الإجابة (`answer_key`) يخزن في جدول سحابي خاص بالخادم فقط وتتم المقارنة خادمياً أثناء التسليم. |
| **3. انتحال محاولات الطلاب (IDOR on Attempts)** | **عالي (High)** | استخدام `attempt_token` عشوائي مشفر بحجم 256-bit يولد خادمياً ولا يقبل الـ API أي طلب حفظ أو تسليم دونه. |
| **4. التلاعب بالوقت (Timer Tampering)** | **عالي (High)** | احتساب الوقت المتبقي خادمياً بالكامل (`server_deadline = started_at + duration`) ورفض أي تسليم يتجاوز الوقت المحدد بغض النظر عن وقت جهاز الطالب. |
| **5. هجمات التخمين والإغراق (Brute Force / Spam)** | **متوسط (Medium)** | تطبيق نظام تحديد معدل الطلبات (Rate Limiting) على الـ API السحابي، واستخدام رموز نشر عشوائية قوية غير قابلة للتخمين. |
| **6. تزوير العلامة (Client-side Scoring)** | **عالي (High)** | منع قبول أي حقل علامة قادم من العميل؛ التصحيح خادمي 100% بناءً على مفتاح الإجابة السري في السحابة. |

---

## 8. الخطة التنفيذية للمراحل القادمة (Roadmap Ahead)

* **المرحلة القادمة فوراً (PHASE 1):** تشغيل واعتماد اختبارات الـ Baseline الحالية وتوثيق تقرير `BASELINE_TEST_REPORT.md`.
* **المرحلة 2 و 3:** تصميم معمارية الـ Hybrid الكاملة وبناء الـ Online Backend المستقل وتجهيز قاعدة بياناته السحابية.
* **المراحل 4 إلى 8:** ربط دورة النشر، تجربة الطالب، الحفظ والتسليم، المزامنة، والحذف الآمن.
* **المرحلة 9 و 10:** اختبارات الاختراق الشاملة، والتأكد التام من جاهزية الإنتاج والتقرير النهائي.

---
**جاهز للانتقال فوراً إلى تنفيذ PHASE 1 (تشغيل وتوثيق اختبارات Baseline) بمجرد اعتمادك لتقرير الفحص.**
